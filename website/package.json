{
  "name": "react-object-to-css-website",
  "version": "1.0.0",
  "dependencies": {
    "nervjs": "^1.2.4"
  },
  "devDependencies": {
    "babel-preset-react": "^6.24.1",
    "gh-pages": "^1.1.0",
    "parcel-bundler": "^1.4.1"
  },
  "scripts": {
    "build": "parcel build index.html --public-url /object-string-to-css",
    "publish": "gh-pages -d dist --repo https://github.com/Vija02/object-string-to-css.git",
    "deploy": "npm run build && npm run publish"
  }
}